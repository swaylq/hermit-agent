# ACCOUNTS.md — 浏览器已登录账号

_占位文件。如果这个 codex hermit 启用 browser-automation（codex 已 bundle browser-use plugin），把浏览器实例已登录的账号列在这里——只记账号标识和状态，**绝对不写密码或 session token**。_

格式参考：

| 平台 | 账号 | 状态 | 备注 |
|------|------|------|------|
| 例：GitHub | example-user | 已登录 | 通过 gh CLI 已认证 |

凭据真实路径在 `TOOLS.md`，这里只做「我知道这个浏览器登过哪些账号」的人类索引。
